extends layout

block content
    h1= title

    div(style='margin-left:20px;margin-top:20px')

        dl
        each link in links
            dt
                a(href=link.href) #{link.title}
            dd #{link.description}    

        else 
            li There are no results        